# @wanex/protocol

Shared TypeScript protocol types for sessions, events, provider fidelity, storage RPC, and resource tickets.

This package must not depend on runtime, provider implementations, gateway, plugin host, desktop, or app code.
