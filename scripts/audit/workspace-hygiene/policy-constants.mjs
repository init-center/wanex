export const skippedDirs = new Set([
  ".git",
  "node_modules",
  "target"
])

export const forbiddenGeneratedDirs = new Set([
  ".cache",
  ".next",
  ".turbo",
  "build",
  "coverage",
  "dist"
])
