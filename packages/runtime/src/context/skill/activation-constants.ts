export const DEFAULT_MAX_INDEXED_FILES = 10
export const DEFAULT_SUPPORTING_DIRECTORIES = [
  "references",
  "templates",
  "scripts",
  "assets"
] as const

export const SKILL_ACTIVATION_TOOL_NAME = "activate_skill" as const
